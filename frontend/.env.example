# URL base de la API del backend (Laravel). Incluye el prefijo /api.
# En desarrollo local:
VITE_API_URL=http://localhost:8000/api

# En producción, apuntá al dominio del backend desplegado (por ejemplo, Railway):
# VITE_API_URL=https://tu-backend.up.railway.app/api
